name: fullstack deployment
on:
  push:
    branches: [main]
jobs:
  build-and-deploy:
    name: deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          
      # 1. 백엔드 빌드 (working-directory 설정 위치 수정)
      - name: Build with Gradle
        working-directory: ./TalleMalle_Backend
        run: |
          chmod +x ./gradlew
          ./gradlew clean bootJar

      - name: Docker Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker Image
        run: |
          docker build -t saralove20/backend:latest ./TalleMalle_Backend
          docker push saralove20/backend:latest
        
      - name: run ssh
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.Host }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.PORT }}
          script: |
              echo test > /tmp/test
